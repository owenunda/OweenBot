name: CI/CD Docker Build and Push

on:
  push:
    branches:
      - main 

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 1. Login a Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }} 
          password: ${{ secrets.DOCKER_PASSWORD }} 

      # 2. Build y Push de la Imagen (Paso Final en GitHub Actions)
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # Asegúrate de que el TAG sea el que Dokploy espera
          tags: ${{ secrets.DOCKER_USERNAME }}/oweenbot:latest 
          
      # 🚨 ¡IMPORTANTE! Hemos ELIMINADO los pasos anteriores de CURL y Webhook.
      # Ahora, Docker Hub enviará la señal a Dokploy una vez que este paso termine.
      
      - name: Finalización del Job
        run: |
           echo "✅ Imagen subida a Docker Hub. Esperando que Docker Hub dispare el Webhook en Dokploy."